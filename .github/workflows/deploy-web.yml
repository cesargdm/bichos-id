name: 'Deploy Web (Cloudflare Workers)'

on:
  push:
    branches:
      - main
    paths:
      - 'apps/next/**'
      - 'packages/app/**'
      - '.github/workflows/deploy-web.yml'

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Typecheck
        run: bun run typecheck
        working-directory: ./apps/next

      # `next build` (invoked by `opennextjs-cloudflare build` below) runs the
      # @sentry/nextjs build plugin, which uploads sourcemaps itself using the
      # org/project already set in next.config.mjs's sentryConfig, as long as
      # SENTRY_AUTH_TOKEN is present — no separate sentry-cli step needed.
      - name: Build and deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ./apps/next
          preCommands: bunx opennextjs-cloudflare build
          command: deploy
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
